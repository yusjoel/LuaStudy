--[[
    标识符 (Identifier)

    由任意字母、数字和下划线构成， 不能以数字开头

    不要使用下划线后跟多个大写字母， 如_VERSION, Lua保留做特殊用途

    单个下划线_, 作为哑变量(Dummy Variable)

    保留字

      and       break     do        else      elseif
      end       false     for       function  goto
      if        in        local     nil       not
      or        repeat    return    then      true
      until     while

    Lua是大小写敏感的
]]