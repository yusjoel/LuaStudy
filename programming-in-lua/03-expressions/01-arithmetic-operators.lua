--[[
    算术操作符:
      -
      +, -, *, /, ^, %

    取模:
      a % b == a - floor(a / b) * b
]]

-- 计算结果的符号与第二个参数相同
-- 1    5   -5
print(365 % 7, -2 % 7, 366 % -7)

-- 实数取模的用途
-- 小数部分, 整数部分, 取模
print(math.pi % 1, math.pi - math.pi % 1, 10 % math.pi)